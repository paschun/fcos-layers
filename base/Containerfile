FROM quay.io/fedora/fedora-coreos:stable

RUN \
    # From the official Fedora repositories, add:
    # tmux, restic, pv
    rpm-ostree install tmux rclone restic pv btop && \
    # Commit the container
    ostree container commit
